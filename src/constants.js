const DEFAULT_OPTIONS = {
    cropOnRender: true,
    cropWidth: 100,
    cropHeight: 100
}

const PREVIEWABLE_FORMATS = [
    "png",
    "jpg",
    "jpeg",
    "gif"
]

export {
    PREVIEWABLE_FORMATS ,
    DEFAULT_OPTIONS
}